module tb_mux5to1;
    // ?????????? ??????
    reg [3:0] D0, D1, D2, D3, D4;
    reg [2:0] A;
    wire [3:0] Q;

    // ??????????? ??? ?????????????
    mux5to1 uut (
        .D0(D0), .D1(D1), .D2(D2), .D3(D3), .D4(D4),
        .A(A), .Q(Q)
    );

    initial begin
        // ????????? ? ???????
        $monitor("Time = %0t | A = %b | Q = %b", $time, A, Q);

        // ??????? ????????? 4-????? ????? ?? ????? ????
        D0 = 4'b1010; // 10 ? ??????????
        D1 = 4'b0011; // 3
        D2 = 4'b1100; // 12
        D3 = 4'b0101; // 5
        D4 = 4'b1111; // 15

        // ??????????? ?????? ??? 0 ?? 4 ? ?????????
        A = 3'b000; #10;
        A = 3'b001; #10;
        A = 3'b010; #10;
        A = 3'b011; #10;
        A = 3'b100; #10;
        
        // ???????????, ?? ???? ?? ?????????? ?????? (????? ???? ????)
        A = 3'b111; #10;

        $finish;
    end
endmodule
